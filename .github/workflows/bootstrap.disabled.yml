name: Bootstrap (Disabled)  

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Disabled workflow
        run: echo "Bootstrap workflow is disabled and will not run automatically."
