name: Meta Dev Launcher

on:
  push:
    paths:
      - 'specs/**'

jobs:
  run-devbox:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Show spec file
        run: cat specs/init.json
