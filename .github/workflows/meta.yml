name: Meta Dev Launcher

on:
  push:
    paths:
      - 'specs/**'

jobs:
  test-run:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "WORKFLOW TRIGGERED SUCCESSFULLY"

